<h1> Add New Artist </h1>

<h1><%= @artist.id ? 'Edit Artist' : 'Add Artist' %></h1>
<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  
  <%= f.submit %>

<% end %> 